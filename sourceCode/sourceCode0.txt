/* 测试词法单元的识别 终点看字符类型 */
{
	a = '\x7E';    /* 16进制转义符 ~ */
	a = '\t';        /* 普通转义符,制表符 */
	a = '\a';	    /* 普通转义符,警告符号*/
	a = '\70';      /* 八进制转义符，8*/
	b = 100087;
	while(a);
	if(b);
	char a = 10;
	int b = 70;

}